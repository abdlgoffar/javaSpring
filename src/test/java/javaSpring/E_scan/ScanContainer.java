package javaSpring.E_scan;

public class ScanContainer {
    //sama seperti import, scan digunakan untuk mengelompokkan file container ke dalam satu file saja.
    //tapi dalam scan container, hanya perlu scan package saja, maka otomatis nantinya semua containernya akan terkelompok
    //untuk melakukan scan cukup menambahkan annotation @ComponentScan.
    //lihat di package D_main_containers, class ConfigurationAppScan.
}
